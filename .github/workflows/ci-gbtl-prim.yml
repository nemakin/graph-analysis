name: CI — build GBTL & run prim_gbtl

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake g++

      - name: Configure & build GBTL
        run: |
          cd gbtl
          mkdir build
          cd build
          cmake ..src
          make -i -j8

      - name: Run prim binary with test dataset
        working-directory: /home/runner/work/graph-analysis/gbtl/build
        run: |
          echo "Running prim_gbtl against small test dataset"
          ./bin/prim_gbtl
